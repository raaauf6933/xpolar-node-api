type Error {
  field: String
  message: String
}

enum UserType {
  SUPER_ADMIN
  ADMIN
}

enum CommonStatus {
  ACT
  DEACT
}

scalar DateTime

type Client {
  id: ID
  name: String
  status: CommonStatus!
  createdAt: DateTime
  updatedAt: DateTime
}

input createClientInput {
  name: String!
  status: CommonStatus!
}

type CreateClient {
  errors: [Error]
  client: Client
}

type Query {
  clients: [Client]
}

type Mutation {
  createClient(input: createClientInput!): CreateClient
}
